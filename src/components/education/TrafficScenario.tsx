/**
 * Componente desactivado por solicitud del usuario.
 * Se mantiene la firma para evitar errores de compilación, pero no renderiza nada.
 */
export function TrafficScenario() {
    return null;
}
